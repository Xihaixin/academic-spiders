# 服务器部署任务

## 架构模式

当前项目存在三种开发模式，如下所示：
1. 本地开发模式 localhost 有两种，第一种是：使用数据库 academicdb
2. 本地开发模式，第二种：使用数据库 academicdb_test
3. 服务器端开发模式，连接远程服务器数据库，设置对应的用户名和密码，访问 pubscholar 

如果我运行程序，使其实现服务器端的开发，如果我还想要在本地调试程序应该如何做？

还是说，如果我运行了程序之后，就无法再去调试代码，不能通过本地测试用例的数据库来做进一步测试 ？

* 设计一个能够控制模式切换的对象：

- 环境变量 + 配置文件



## 日志记录

三种开发模式对应的日志应该设置隔离，其日志内容不互通。

本地两种开发模式，虽然只有所使用的数据库不一致，但是两者还是存在定位上的一些差别。

另外，服务器端的日志系统还应该具备日志轮转的能力，以应对持续增长的数据量，因此我想要将数据存储在另一个服务器路径上：\\192.168.3.8\xhx\academic-spiders\log


## 数据管道 JsonExportPipeline 问题

数据没有经过它，还是说该数据管道存在问题


## 服务器部署调整 [重要]

当前程序主要还是去获取两大核心期刊目录对应的数据，但是我们想要获取到所有的中文期刊数据，可以优先获取两大核心期刊目录。

目前程序主要还是设置 collection = ["北大核心", "南大核心"]，所以这里需要进行修正。